load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "logtools",
    hdrs = [
        "include/logtools/logger_tools.h"
    ],
    includes = ["logtools/include"],
    visibility = ["//visibility:public"],
    deps = [
        "@spdlog//:spdlog",
        "@fmt",
    ],
    strip_include_prefix="include"
)
