# This file is configured at cmake time, loaded at cpack time.

# NSIS specific settings
if(CPACK_GENERATOR MATCHES "NSIS")
  set(CPACK_NSIS_MUI_ICON "@CMAKE_SOURCE_DIR@")
  set(CPACK_NSIS_HELP_LINK "https://github.com/frank1789/LsDynaToAPDL.git")
  set(CPACK_NSIS_URL_INFO_ABOUT "https://github.com/frank1789/LsDynaToAPDL.git")
  set(CPACK_NSIS_INSTALLED_ICON_NAME "bin\\\\LsDynaToAPDL.exe")
  set(CPACK_NSIS_MENU_LINKS
    "https://github.com/frank1789/LsDynaToAPDL.git")
  set(CPACK_NSIS_MODIFY_PATH ON)
endif(CPACK_GENERATOR MATCHES "NSIS")

if("${CPACK_GENERATOR}" STREQUAL "PackageMaker")
  set(CPACK_PACKAGE_DEFAULT_LOCATION "/Applications")
endif("${CPACK_GENERATOR}" STREQUAL "PackageMaker")