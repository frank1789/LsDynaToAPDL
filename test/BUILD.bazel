load("@rules_cc//cc:defs.bzl", "cc_test")

filegroup(
    name = "resources",
    srcs = glob(["**/*.k"]),
    visibility = ["//visibility:public"],
)

cc_test(
    name = "LsDynaToApdlTests",
    srcs = glob(["**/*.cc"]),
    copts = ["-Iexternal/gtest/include"],
    data = [":resources"],
    deps = [
        "//src/apdl",
        "//src/common",
        "//src/lsdyna",
        "@com_google_googletest//:gtest",
        "@com_google_googletest//:gtest_main",
        "@fmt",
    ],
)
